{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "npm run build:association",
        "build:association": "sass resources/sass/association:public/css/association --no-source-map",
        "watch:association": "sass --watch resources/sass/association:public/css/association --no-source-map",
        "test:a11y": "playwright test"
    },
    "devDependencies": {
        "@axe-core/playwright": "^4.12.1",
        "@playwright/test": "^1.61.1",
        "laravel-vite-plugin": "^1.0.0",
        "sass": "^1.101.0",
        "vite": "^6.0.0"
    },
    "dependencies": {
        "@popperjs/core": "^2.11.8",
        "axios": "^1.18.1",
        "bootstrap": "^5.3.8",
        "lodash": "^4.18.1"
    }
}
